{#
 # This file is part of the OpenMiamMiam project.
 # (c) Isics <contact@isics.fr>
 #
 # This source file is subject to the AGPL v3 license that is bundled
 # with this source code in the file LICENSE.
 #}
{% extends "IsicsOpenMiamMiamBundle::minimal.html.twig" %}

{% form_theme form _self 'IsicsOpenMiamMiamBundle::form.html.twig' %}

{% block content %}
    <div class="row">
        <div class="col-md-6 col-md-offset-3">
            <div class="panel panel-primary">
                <div class="panel-heading">
                    <h1 class="panel-title">{{ 'user.edit.title'|trans }}</h1>
                </div>
                <div class="panel-body">
                    {{ form_start(form) }}
                        <div class="row">
                            <div class="col-md-6">
                                {{ form_row(form.firstname, {'label': 'user.edit.firstname'|trans, 'label_as_placeholder': true, 'attr': {'class': 'input-lg'}}) }}
                            </div>
                            <div class="col-md-6">
                                {{ form_row(form.lastname, {'label': 'user.edit.lastname'|trans, 'label_as_placeholder': true, 'attr': {'class': 'input-lg'}}) }}
                            </div>
                        </div>
                        {{ form_row(form.address1, {'label': 'user.edit.address1'|trans, 'label_as_placeholder': true}) }}
                        {{ form_row(form.address2, {'label': 'user.edit.address2'|trans, 'label_as_placeholder': true}) }}
                        <div class="row">
                            <div class="col-md-4">
                                {{ form_row(form.zipcode, {'label': 'user.edit.zipcode'|trans, 'label_as_placeholder': true}) }}
                            </div>
                            <div class="col-md-8">
                                {{ form_row(form.city, {'label': 'user.edit.city'|trans, 'label_as_placeholder': true}) }}
                            </div>
                        </div>

                    <a href="{{ path('fos_user_change_password') }}">{{ 'user.change_password'|trans }}</a>
                    </div>
                    <div class="panel-footer text-right">
                        <a href="{{ path('open_miam_miam.homepage') }}" class="btn btn-default btn-lg">
                            {{ 'user.edit.cancel'|trans }}
                        </a>
                        <button type="submit" class="btn btn-primary btn-lg">
                            <span class="glyphicon glyphicon-circle-arrow-right"></span>
                            {{ 'user.edit.submit'|trans }}
                        </button>
                    </div>
                </div>
            {{ form_end(form) }}
        </div>
    </div>
{% endblock content %}
