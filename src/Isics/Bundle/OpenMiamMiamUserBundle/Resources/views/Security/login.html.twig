{% extends "IsicsOpenMiamMiamBundle:Admin:base.html.twig" %}

{% block content %}
    <div class="row">
        <div class="col-md-4 col-md-offset-4">
            <h1>{% trans %}admin.login.title{% endtrans %}</h1>

            {% if error %}
                <div class="alert alert-danger">{{ error|trans({}, 'FOSUserBundle') }}</div>
            {% endif %}

            <form action="{{ path("fos_user_security_check") }}" method="post">
                <input type="hidden" name="_csrf_token" value="{{ csrf_token }}" />

                <input type="text" id="username" name="_username" value="{{ last_username }}" required="required" class="form-control" placeholder="{% trans %}admin.login.email.placeholder{% endtrans %}" />

                <input type="password" id="password" name="_password" required="required" class="form-control" placeholder="{% trans %}admin.login.password.placeholder{% endtrans %}" />

                <label for="remember_me">
                    <input type="checkbox" id="remember_me" name="_remember_me" value="on" />
                    {{ 'security.login.remember_me'|trans({}, 'FOSUserBundle') }}
                </label>

                <input type="submit" id="_submit" name="_submit" value="{{ 'admin.login.submit'|trans }}" class="btn btn-lg btn-primary btn-block" />
            </form>
            <a href="{{ path("fos_user_registration_register") }}">account.register</a>
        </div>
    </div>
{% endblock content %}
